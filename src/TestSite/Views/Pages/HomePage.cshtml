@model TestSite.Models.Pages.HomePage

@using Blend.Optimizely

@{
    var richTextBlocks = await Model.Body.AsContentAsync<TestSite.Models.Blocks.RichTextBlock>();
}
<h1>Content:</h1>

@if (Model.Body.HasValue())
{
    <p>No body to display.</p>
}

@foreach (var block in richTextBlocks)
{
    <div>
        @Html.XhtmlString(block.Body)
    </div>
}


@Html.PropertyFor(x => x.Links)

@if (Model.TestUrl.HasValue())
{
    <a href="@Model.TestUrl.ResolveUrl()">Test URL</a>
}